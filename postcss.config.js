module.exports = {
  plugins: [
    // 'postcss-preset-env',
    // eslint-disable-next-line global-require
    require('postcss-filter-gradient'),
    require('autoprefixer'),
    // require('autoprefixer'),
  ],
};
